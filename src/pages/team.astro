---
import Footer from '../components/site/Footer.astro';
import Header from '../components/site/Header.astro';
import SmoothScroll from '../components/site/SmoothScroll.jsx';
import BaseLayout from '../layouts/BaseLayout.astro';
import { contactDetails, teamMembers } from '../data/aurea.js';
---

<BaseLayout
  title="Team — AUREA"
  description="Meet the six people behind AUREA — software engineers, data scientists, and strategists building better digital platforms."
  canonical="https://coppsary.com/team"
>
  <a href="#main-content" class="skip-link">Skip to main content</a>
  <div class="site-shell">
    <SmoothScroll client:load />
    <Header pathname={Astro.url.pathname} />

    <main id="main-content">

      <section class="team-page__hero">
        <div class="team-page__hero-inner">
          <div class="section-label">(01) Team</div>
          <h1 class="display-title">Six people.<br/>One standard.</h1>
          <p>
            AUREA is built by a compact team of software engineers, data scientists, and analysts.
            Everyone is hands-on — we build the things we ship.
          </p>
        </div>
      </section>

      <div class="team-page__grid">
        <div class="team-detail-grid">
          {teamMembers.map((member) => (
            <article class="team-detail-card">
              <div class="team-detail-card__media">
                <img
                  src={member.image}
                  alt={member.name}
                  class="team-detail-card__image"
                  loading="lazy"
                  decoding="async"
                />
              </div>
              <div class="team-detail-card__body">
                <span class="team-detail-card__role">{member.role}</span>
                <h2 class="team-detail-card__name">{member.name}</h2>
                <p class="team-detail-card__desc">{member.description}</p>
              </div>
            </article>
          ))}
        </div>
      </div>

      <section class="contact-section">
        <div class="contact-section__dot" aria-hidden="true"></div>
        <div class="contact-section__content">
          <div class="section-label section-label--dark">(02) Work with us</div>
          <h2>{contactDetails.headline}</h2>
          <div class="contact-section__links" aria-label="Direct contact links">
            <a href={contactDetails.email} class="contact-section__link">
              <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                <path d="M4 6h16v12H4z"></path>
                <path d="m4 7 8 6 8-6"></path>
              </svg>
              {contactDetails.emailLabel}
            </a>
            <a href={contactDetails.telegram} target="_blank" rel="noreferrer" class="contact-section__link">
              <svg viewBox="0 0 24 24" aria-hidden="true" fill="currentColor">
                <path d="M21.4 4.6c.3-.9-.6-1.7-1.5-1.4L3.8 9.4c-1 .4-1 1.8.1 2.1l4.1 1.3 1.6 5c.3 1 1.6 1.1 2.1.2l2.3-3.2 4.4 3.3c.8.6 1.9.2 2.1-.8l.9-12.7ZM9.1 12.1l8.4-5.2-6.9 6.7-.3 2.7-1.2-4.2Z"></path>
              </svg>
              {contactDetails.telegramLabel}
            </a>
            <a href={contactDetails.phone} class="contact-section__link">
              <svg viewBox="0 0 24 24" aria-hidden="true" fill="none" stroke="currentColor" stroke-width="1.8" stroke-linecap="round" stroke-linejoin="round">
                <path d="M22 16.9v3a2 2 0 0 1-2.2 2 19.8 19.8 0 0 1-8.6-3.1 19.5 19.5 0 0 1-6-6A19.8 19.8 0 0 1 2.1 4.2 2 2 0 0 1 4.1 2h3a2 2 0 0 1 2 1.7l.5 3a2 2 0 0 1-.6 1.8l-1.3 1.3a16 16 0 0 0 6.4 6.4l1.3-1.3a2 2 0 0 1 1.8-.6l3 .5A2 2 0 0 1 22 16.9z"></path>
              </svg>
              {contactDetails.phoneLabel}
            </a>
          </div>
        </div>
      </section>
    </main>

    <Footer />
  </div>
</BaseLayout>
